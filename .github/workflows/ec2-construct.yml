name: EC2 Construct 

on: 
  workflow_dispatch: 
    inputs:
      compose-file-path:
        description: "Docker Compose文件路径"
        required: true
        default: "./resumeAPI/docker-compose.yml"
  
env: 
  AWS_REGION: ap-southeast-2 
 
jobs: 
  terraform: 
    name: Apply Terraform configuration 
    runs-on: ubuntu-latest 
    defaults: 
      run: 
        working-directory: ./ec2/ 

    steps: 
      - name: Checkout terraform code 
        uses: actions/checkout@v4

      - name: Set up Terraform 
        uses: hashicorp/setup-terraform@v2 
        with: 
          terraform_version: 1.0.0 

      - name: Configure AWS Credentials 
        uses: aws-actions/configure-aws-credentials@v2
        with: 
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }} 
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }} 
          aws-region: ${{ env.AWS_REGION }} 

      - name: Terraform Init & Plan 
        run: |
          terraform init
          terraform plan 

      - name: Terraform Apply 
        if: github.ref == 'refs/heads/main' 
        run: terraform apply -auto-approve 

      - name: Get EC2 IP 
        run: |
          terraform output -raw instance_public_ip > ip.txt
           echo "===== IP 文件内容 ====="
           cat ip.txt  # 调试输出
           echo "======================="
          # 使用awk提取纯IP（更可靠的方式）
          IP=$(awk 'match($0, /[0-9]+\.[0-9]+\.[0-9]+\.[0-9]+/) { print substr($0, RSTART, RLENGTH) }' ip.txt)
          
          # 验证IP格式
          if [[ ! $IP =~ ^[0-9]+\.[0-9]+\.[0-9]+\.[0-9]+$ ]]; then
            echo "ERROR: Invalid IP format: $IP"
            exit 1
          fi
          
          # 正确设置GITHUB_OUTPUT（新语法）
          echo "IP_ADDRESS=$IP" >> $GITHUB_ENV
          echo "ip_address=$IP" >> $GITHUB_OUTPUT
          
          echo "Extracted IP: $IP"

  trigger-remote-build:
    name: Trigger remote repository build
    needs: terraform
    runs-on: ubuntu-latest
    defaults: 
      run: 
        working-directory: ./ec2/ 
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Debug variables
        run: |
          echo "IP: ${{ steps.ip.outputs.ip_address }}"
          echo "Compose Path: ${{ github.event.inputs.compose-file-path }}"

      - name: Trigger remote workflow
        uses: benc-uk/workflow-dispatch@v1
        with:
          workflow: build-push.yml # 目标工作流文件名
          repo: QiushiZhou/resumeAPI # 目标仓库
          token: ${{ secrets.PAT }} # 需要个人访问令牌(PAT)
          ref: main # 目标分支
          inputs: |
            {
              "VITE_SERVER_HOST": "http://${{ steps.get-ip.outputs.ip_address }}",
              "DOCKER_COMPOSE_FILE": "${{ github.event.inputs.compose-file-path }}"  
            }
